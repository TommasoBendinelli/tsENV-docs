\documentclass[11pt]{article}
\usepackage{manual_docs_style}
\externaldocument{build/appendix_agents}[appendix_agents.pdf]

\begin{document}

\docTitle{Agent Profiles Reference}
\phantomsection\label{docstart:agent_profiles}
This document matches the following two files: \code{shared/platform.json} and the agent-specific metadata in \code{shared/config/agents.json}.
They are used from the agentic rollout stage onward.

\section*{Platform Schema}
\code{shared/platform.json} must contain a non-empty JSON object keyed by \code{platform_id}.
Supported platform ids are \code{codex}, \code{gemini-cli}, \code{claude-code}, and \code{opencode}.

Each platform entry must define exactly these fields:
\begin{itemize}
\item \code{name}: non-empty string matching the \code{platform_id}
\item \code{npm_package}: non-empty string containing the pinned npm package spec used to build the install command
\item \code{required_environmental_variable}: object mapping non-empty environment-variable names to non-empty shell-command strings; the object may be empty
\item \code{default_tools}: object containing tools shared by all agents of this platform; currently empty
\end{itemize}

\section*{Agent Schema}
\code{shared/config/agents.json} must contain a non-empty JSON array of agent profile objects.
Unless explicitly marked optional, all fields are required.

\begingroup
\footnotesize
\renewcommand{\arraystretch}{1.12}
\setlength{\tabcolsep}{4pt}
\begin{longtable}{|P{0.24\textwidth}|P{0.30\textwidth}|P{0.36\textwidth}|}
\caption{Agent profile fields.}\\
\hline
\textbf{Field} & \textbf{Type / allowed values} & \textbf{Meaning} \\
\hline
\endfirsthead
\hline
\textbf{Field} & \textbf{Type / allowed values} & \textbf{Meaning} \\
\hline
\endhead
\code{agent_id} & non-empty string & Unique identifier for the agent profile. \\
\hline
\code{platform_id} & \code{codex}, \code{gemini-cli}, \code{claude-code}, or \code{opencode} & Platform used to run the agent. \\
\hline
\code{cost.input_token} & non-negative number & Input-token cost per 1M tokens. \\
\hline
\code{cost.cached_token} & non-negative number & Cached-input-token cost per 1M tokens. \\
\hline
\code{cost.completion_token} & non-negative number & Completion-token cost per 1M tokens. \\
\hline
\code{support_images} & boolean & Whether the agent can inspect images. \\
\hline
\code{available_tools} & object keyed by tool name & Tools available to the agent. \\
\hline
\code{available_tools.<tool>.parse_method} & \code{false}, \code{tree_sitter_bash}, or \code{file_path} & How tool calls are parsed during trajectory evaluation. \\
\hline
\code{available_tools.<tool>.subcommands} & list of non-empty strings & Shell subcommands analyzed in detail. \\
\hline
\code{model_name} & non-empty string & Model name used by the platform. \\
\hline
\code{reasoning} & \code{high}, \code{medium}, \code{low}, or \code{null} & Reasoning-effort setting, when supported by the platform; otherwise \code{null}. \\
\hline
\code{paper_facing_name} & non-empty string or omitted & Optional display name used in generated paper tables. \\
\hline
\end{longtable}
\endgroup

If \code{paper_facing_name} is omitted, \code{.tex} tables generation falls back to a display name derived from \code{model_name}

The full agent platform and profile catalog is collected in the paper appendix, under \hyperref[docstart:appendix_agents]{Appendix: Agent Platforms and Profiles}.

\end{document}
